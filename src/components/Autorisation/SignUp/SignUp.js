import React from "react";
import Form from "../Form/Form";

const SignUp = ({ title }) => {
  return (
    <div>
      <div>
        <Form
          title={title}
          // handleClick={handlLogin}
        />
      </div>
    </div>
  );
};

export default SignUp;
