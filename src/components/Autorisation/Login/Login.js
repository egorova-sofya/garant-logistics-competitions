import React from "react";
import Form from "../Form/Form";

const Login = ({ title }) => {
  return (
    <div>
      <Form
        title={title}
        // handleClick={handlLogin}
      />
    </div>
  );
};

export default Login;
